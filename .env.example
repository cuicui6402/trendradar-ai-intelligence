# TrendRadar 环境配置模板
# 复制本文件并重命名为 .env，填入你的真实密钥。
# 所有配置均为可选，不填时项目以降级模式运行。

# GitHub 个人访问令牌（可选，不填则使用公开接口，有速率限制）
GITHUB_TOKEN=

# LLM API 配置（可选）
# 不填时使用模板模式生成报告（仍然可用，包含完整 9 章节）
# 填写后 Agent 调用 OpenAI 兼容 API 生成深度分析报告
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
